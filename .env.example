# Base URL of the shop BFF (board-game-shop-api).
# BROWSER-side: this URL is baked into the client bundle and must be reachable
# from the user's browser — not a docker-internal hostname.
VITE_SHOP_API_URL=http://localhost:3000
